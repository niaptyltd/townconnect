import { PageState } from "@/components/ui/page-state";

export default function Loading() {
  return (
    <PageState
      title="Loading TownConnect"
      description="We are preparing the latest listings, account state and town data for you."
    />
  );
}
